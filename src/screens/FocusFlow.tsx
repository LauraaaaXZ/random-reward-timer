import React,{useEffect,useMemo,useRef,useState}from'react';import{Alert,AppState,Pressable,ScrollView,StyleSheet,Text,View}from'react-native';import{listTasks}from'../data/taskRepository';import{applyReward,Wallet}from'../data/economyRepository';import{finishSession,startSession}from'../data/sessionRepository';import{isHoliday}from'../data/holidayRepository';import{grantAnnualLeaveMinutes,recordHolidayWork}from'../data/leaveRepository';import{addDrawProgress}from'../data/drawBankRepository';import{completeAcceptedInvitation}from'../data/taskInvitationRepository';import{chooseRestWithCoin,scheduleNextDrawAfter,startRandomRest,RestState}from'../data/restRepository';import{GAME_CONFIG}from'../config/gameConfig';import{eligibleTasks,difficultyTasks}from'../engine/eligibility';import{commitmentMinutes,weightedTaskDraw}from'../engine/random';import{calculateSessionReward}from'../engine/rewards';import{taskCompletionReward}from'../engine/rewardPools';import{drawInvitationBonus,InvitationBonus}from'../engine/invitationBonus';import{DrawMode,SessionPool,Task}from'../domain/types';import{applyTaskWork,markTaskCompleted}from'../services/taskService';
type Phase='draw'|'timer'|'settlement'|'reward';type SelectionMode='random_task'|'fixed_task'|'fixed_time';type RewardResult={coin:number;xp:number;multiplier:number;overtimeMinutes:number;multiTask:number;compLeaveDays?:number;drawProgress?:number;taskCoinBonus?:number;invitationBonus?:InvitationBonus;wallet?:Wallet;rest?:RestState|null;remainingMinutes?:number;progressPercent?:number};function localDateKey(date=new Date()){return`${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,'0')}-${String(date.getDate()).padStart(2,'0')}`}
export function FocusFlow({pool=30,freeMinutes=30,initialTaskId,workMode='normal',onClose}:{pool?:SessionPool;freeMinutes?:number;initialTaskId?:string;workMode?:'normal'|'low_intensity';onClose:()=>void}){const[phase,setPhase]=useState<Phase>('draw'),[selectionMode,setSelectionMode]=useState<SelectionMode>('random_task'),[mode,setMode]=useState<DrawMode>('normal'),[task,setTask]=useState<Task|null>(null),[fixedCandidates,setFixedCandidates]=useState<Task[]>([]),[targetSeconds,setTargetSeconds]=useState(0),[chosenMinutes,setChosenMinutes]=useState<number>(typeof pool==='number'?pool:90),[elapsedSeconds,setElapsedSeconds]=useState(0);const startedAtMs=useRef<number|null>(null);const[completed,setCompleted]=useState(false),[additionalTasks,setAdditionalTasks]=useState<Task[]>([]),[completionCandidates,setCompletionCandidates]=useState<Task[]>([]),[sessionId,setSessionId]=useState<string|null>(null),[reward,setReward]=useState<RewardResult|null>(null),[busy,setBusy]=useState(false),[notice,setNotice]=useState<string|null>(null);function syncElapsed(){if(startedAtMs.current!==null)setElapsedSeconds(Math.max(0,Math.floor((Date.now()-startedAtMs.current)/1000)))}useEffect(()=>{if(phase!=='timer')return;syncElapsed();const id=setInterval(syncElapsed,1000),sub=AppState.addEventListener('change',s=>{if(s==='active')syncElapsed()});return()=>{clearInterval(id);sub.remove()}},[phase]);useEffect(()=>{if(phase==='draw'&&selectionMode==='fixed_task')listTasks().then(t=>{const base=eligibleTasks(t);setFixedCandidates(workMode==='low_intensity'?base.filter(x=>x.difficulty==='easy'):base)}).catch(e=>Alert.alert('Could not load tasks',String(e)))},[phase,selectionMode,workMode]);useEffect(()=>{if(!initialTaskId||phase!=='draw')return;listTasks().then(ts=>{const s=ts.find(t=>t.id===initialTaskId&&t.status==='active');if(s){setSelectionMode('fixed_task');setFixedCandidates(eligibleTasks(ts));randomizeTime(s)}})},[initialTaskId]);const remaining=targetSeconds-elapsedSeconds,overtime=Math.max(0,-remaining),displaySeconds=Math.abs(remaining),clock=useMemo(()=>`${String(Math.floor(displaySeconds/60)).padStart(2,'0')}:${String(displaySeconds%60).padStart(2,'0')}`,[displaySeconds]);function randomizeTime(s:Task){setTask(s);setTargetSeconds(commitmentMinutes(s,pool,freeMinutes)*60)}async function draw(){const ts=await listTasks(),base=mode==='normal'?eligibleTasks(ts):difficultyTasks(ts),cs=workMode==='low_intensity'?base.filter(x=>x.difficulty==='easy'):base,s=weightedTaskDraw(cs);if(!s)return Alert.alert('No eligible tasks');const secs=selectionMode==='fixed_time'?chosenMinutes*60:commitmentMinutes(s,pool,freeMinutes)*60;setTask(s);setTargetSeconds(secs);setNotice(`✓ New task confirmed · ${s.name} · ${Math.round(secs/60)} min`);setTimeout(()=>setNotice(null),4000)}async function start(){if(!task)return;setSessionId(await startSession({taskId:task.id,pool,drawMode:mode,commitmentMinutes:Math.round(targetSeconds/60),workMode}));startedAtMs.current=Date.now();setElapsedSeconds(0);setPhase('timer')}async function finish(){syncElapsed();const ts=await listTasks();setCompletionCandidates(eligibleTasks(ts).filter(c=>c.id!==task?.id));setPhase('settlement')}function toggleAdditional(c:Task){setAdditionalTasks(x=>x.some(i=>i.id===c.id)?x.filter(i=>i.id!==c.id):x.length>=2?x:[...x,c])}
async function settle(){if(!task||!sessionId||busy)return;setBusy(true);try{const workedMinutes=Math.max(1,Math.ceil(elapsedSeconds/60));await finishSession(sessionId,workedMinutes);let updatedTask:Task|null=null;if(completed)await markTaskCompleted(task.id);else updatedTask=await applyTaskWork(task.id,workedMinutes);for(const x of additionalTasks)await markTaskCompleted(x.id);const done=[...(completed?[task]:[]),...additionalTasks],count=done.length,calc=calculateSessionReward({actualMinutes:workedMinutes,commitmentMinutes:Math.round(targetSeconds/60),difficulty:task.difficulty,drawMode:mode,completedTaskCount:count});let drawProgress=0,taskCoinBonus=0;if(count){const per=Math.max(5,workedMinutes/count);for(const t of done){const r=taskCompletionReward(t.difficulty,per);drawProgress+=r.drawProgress;taskCoinBonus+=r.coin}drawProgress=Number(drawProgress.toFixed(2));await addDrawProgress(drawProgress,`session:${sessionId}:tasks`)}let invitationBonus:InvitationBonus|undefined,invitationCoin=0;if(initialTaskId&&completed&&task.id===initialTaskId&&await completeAcceptedInvitation(task.id)){invitationBonus=drawInvitationBonus();if(invitationBonus.type==='coin')invitationCoin=invitationBonus.amount;else if(invitationBonus.type==='annual_leave_minutes')await grantAnnualLeaveMinutes(invitationBonus.minutes);else await addDrawProgress(invitationBonus.amount,`session:${sessionId}:invitation`)}const wallet=await applyReward({sessionId,source:invitationBonus?'invited_task':'focus_session',coin:calc.coin+taskCoinBonus+invitationCoin,xp:calc.xp,multiplier:calc.multiplier}),today=localDateKey(),compLeaveDays=await isHoliday(today)?await recordHolidayWork(workedMinutes,today,sessionId):0,rest=count?await startRandomRest(Math.random,new Date(),sessionId):null,remainingMinutes=completed?0:updatedTask?.remainingMinutes,progressPercent=completed?100:updatedTask?Math.min(100,Math.round((1-updatedTask.remainingMinutes/updatedTask.estimatedMinutes)*100)):undefined;setReward({...calc,coin:calc.coin+taskCoinBonus+invitationCoin,wallet,compLeaveDays,drawProgress,taskCoinBonus,invitationBonus,rest,remainingMinutes,progressPercent});setNotice(completed?`✓ Task completed · ${task.name} · ${workedMinutes} min worked${workMode==='low_intensity'?` · ${Math.round(workedMinutes*.5)} min capacity credit`:''}`:`✓ Progress saved · ${task.name} · ${progressPercent??0}% · ${remainingMinutes??0}/${task.estimatedMinutes} min remaining${workMode==='low_intensity'?` · ${Math.round(workedMinutes*.5)} min capacity credit`:''}`);setPhase('reward')}catch(e){Alert.alert('Could not settle',e instanceof Error?e.message:String(e))}finally{setBusy(false)}}
if(phase==='draw')return <ScrollView contentContainerStyle={styles.settlement}>{notice?<View style={styles.notice}><Text style={styles.noticeText}>{notice}</Text></View>:null}<Text style={styles.kicker}>{workMode==='low_intensity'?'LOW-INTENSITY WORK · 50% CAPACITY CREDIT':initialTaskId?'RANDOM INVITATION':`${pool} MIN POOL`}</Text><Text style={styles.title}>{initialTaskId?'Invitation accepted':'Choose how to start'}</Text>{initialTaskId&&task?<View style={styles.card}><Text style={styles.task}>{task.name}</Text><Text style={styles.meta}>Random commitment · {Math.round(targetSeconds/60)} min</Text><Pressable onPress={start} style={styles.primary}><Text style={styles.primaryText}>Start</Text></Pressable></View>:<><View style={styles.modeRow}><Pressable onPress={()=>{setSelectionMode('random_task');setTask(null)}} style={[styles.chip,selectionMode==='random_task'&&styles.selected]}><Text>Random task + time</Text></Pressable><Pressable onPress={()=>{setSelectionMode('fixed_task');setTask(null)}} style={[styles.chip,selectionMode==='fixed_task'&&styles.selected]}><Text>Choose task + random time</Text></Pressable><Pressable onPress={()=>{setSelectionMode('fixed_time');setTask(null);setTargetSeconds(chosenMinutes*60)}} style={[styles.chip,selectionMode==='fixed_time'&&styles.selected]}><Text>Choose time + random task</Text></Pressable></View>{selectionMode==='fixed_task'?<>{fixedCandidates.map(c=><Pressable key={c.id} onPress={()=>randomizeTime(c)} style={styles.option}><Text style={styles.optionTitle}>{c.name} · {c.remainingMinutes}/{c.estimatedMinutes} min left</Text></Pressable>)}{task?<TaskResult task={task} minutes={Math.round(targetSeconds/60)} onStart={start} onReroll={()=>randomizeTime(task)}/>:null}</>:selectionMode==='fixed_time'?<>{task?<TaskResult task={task} minutes={chosenMinutes} onStart={start} onReroll={draw}/>:<Pressable onPress={draw} style={styles.primary}><Text style={styles.primaryText}>DRAW TASK</Text></Pressable>}</>:task?<TaskResult task={task} minutes={Math.round(targetSeconds/60)} onStart={start} onReroll={draw}/>:<Pressable onPress={draw} style={styles.primary}><Text style={styles.primaryText}>DRAW</Text></Pressable>}</>}<Pressable onPress={onClose}><Text style={styles.close}>Cancel</Text></Pressable></ScrollView>;if(phase==='timer')return <View style={styles.page}><Text style={styles.kicker}>{overtime?'OVERTIME':workMode==='low_intensity'?'LOW-INTENSITY WORK':'FOCUS'}</Text><Text style={styles.task}>{task?.name}</Text><Text style={styles.timer}>{overtime?'+':''}{clock}</Text><Pressable onPress={finish} style={styles.primary}><Text style={styles.primaryText}>{remaining>0?'End Early':'Finish'}</Text></Pressable></View>;if(phase==='settlement')return <ScrollView contentContainerStyle={styles.settlement}><Text style={styles.kicker}>SETTLEMENT</Text><Text style={styles.title}>{Math.ceil(elapsedSeconds/60)} min worked</Text><Pressable onPress={()=>setCompleted(false)} style={[styles.option,!completed&&styles.optionSelected]}><Text>Save progress only</Text></Pressable><Pressable onPress={()=>setCompleted(true)} style={[styles.option,completed&&styles.optionSelected]}><Text>Primary task completed ✓</Text></Pressable><Pressable disabled={busy} onPress={settle} style={[styles.primary,busy&&styles.disabled]}><Text style={styles.primaryText}>{busy?'Processing…':'Confirm'}</Text></Pressable></ScrollView>;async function chooseRest(m:number){try{const rest=await chooseRestWithCoin(m);setReward(r=>r?{...r,rest}:r)}catch(e){Alert.alert('Cannot choose rest',String(e))}}async function setNextDrawDelay(m:number){try{const rest=await scheduleNextDrawAfter(m);setReward(r=>r?{...r,rest}:r)}catch(e){Alert.alert('Cannot schedule',String(e))}}return <ScrollView contentContainerStyle={styles.settlement}>{notice?<View style={styles.notice}><Text style={styles.noticeText}>{notice}</Text></View>:null}<Text style={styles.kicker}>COMPLETE ✓</Text><Text style={styles.reward}>+ {reward?.coin??0} Coin</Text><Text style={styles.reward}>+ {reward?.xp??0} XP</Text>{reward?.remainingMinutes!==undefined?<Text style={styles.comp}>Task progress · {reward.progressPercent}% · {reward.remainingMinutes}/{task?.estimatedMinutes} min remaining</Text>:null}<Pressable onPress={onClose} style={styles.primary}><Text style={styles.primaryText}>Back to Today</Text></Pressable></ScrollView>}
function TaskResult({task,minutes,onStart,onReroll}:{task:Task;minutes:number;onStart:()=>void;onReroll:()=>void}){const pct=Math.min(100,Math.round((1-task.remainingMinutes/task.estimatedMinutes)*100));return <View style={styles.card}><Text style={styles.task}>{task.name}</Text><Text style={styles.meta}>{task.difficulty.replace('_',' ')} · focus {minutes} min · {task.remainingMinutes}/{task.estimatedMinutes} min left · {pct}% complete</Text><Pressable onPress={onStart} style={styles.primary}><Text style={styles.primaryText}>Start</Text></Pressable><Pressable onPress={onReroll} style={styles.secondary}><Text>↻ Reroll</Text></Pressable></View>}
const styles=StyleSheet.create({notice:{backgroundColor:'#172033',borderRadius:16,padding:14},noticeText:{color:'white',fontWeight:'800'},disabled:{opacity:.45},page:{flex:1,padding:24,justifyContent:'center',gap:18,backgroundColor:'#F7F8FA'},settlement:{padding:24,paddingBottom:70,gap:14,backgroundColor:'#F7F8FA'},kicker:{fontSize:12,fontWeight:'800',letterSpacing:1.5,opacity:.5},title:{fontSize:30,fontWeight:'800'},modeRow:{flexDirection:'row',flexWrap:'wrap',gap:8},chip:{padding:12,borderRadius:999,backgroundColor:'#E9EDF4'},selected:{backgroundColor:'#C9D9FF'},card:{padding:20,borderRadius:24,backgroundColor:'white',gap:14},task:{fontSize:24,fontWeight:'800'},meta:{opacity:.55},timer:{fontSize:64,fontWeight:'800'},primary:{minHeight:54,borderRadius:17,backgroundColor:'#246BFD',alignItems:'center',justifyContent:'center'},primaryText:{color:'white',fontWeight:'900'},secondary:{minHeight:46,alignItems:'center',justifyContent:'center'},close:{textAlign:'center',opacity:.5},option:{padding:16,borderRadius:16,backgroundColor:'white'},optionSelected:{borderWidth:2,borderColor:'#246BFD'},optionTitle:{fontWeight:'800'},reward:{fontSize:28,fontWeight:'900'},comp:{fontSize:20,fontWeight:'900'}});