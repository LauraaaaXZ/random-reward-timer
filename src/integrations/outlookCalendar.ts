import { CalendarProvider, ExternalCalendarEvent } from './calendarProvider';
const GRAPH_ROOT='https://graph.microsoft.com/v1.0';type GraphDateTime={dateTime:string;timeZone:string};type GraphCalendar={id:string;name?:string|null};type GraphCalendarsPage={value:GraphCalendar[];'@odata.nextLink'?:string};type GraphEvent={id:string;subject?:string|null;start:GraphDateTime;end:GraphDateTime;isCancelled?:boolean;isAllDay?:boolean;showAs?:string;lastModifiedDateTime?:string};type GraphEventsPage={value:GraphEvent[];'@odata.nextLink'?:string};
function asUtcIso(dt:GraphDateTime){if(/[zZ]|[+-]\d\d:\d\d$/.test(dt.dateTime))return new Date(dt.dateTime).toISOString();return new Date(`${dt.dateTime}Z`).toISOString();}
async function graphJson<T>(url:string,accessToken:string):Promise<T>{const response=await fetch(url,{headers:{Authorization:`Bearer ${accessToken}`,Prefer:'outlook.timezone="UTC"'}});if(!response.ok){const detail=await response.text();throw new Error(`Outlook calendar sync failed (${response.status}): ${detail.slice(0,240)}`);}return response.json() as Promise<T>;}
async function listCalendars(accessToken:string){const calendars:GraphCalendar[]=[];let url:string|undefined=`${GRAPH_ROOT}/me/calendars?$select=id,name&$top=100`;while(url){const page:GraphCalendarsPage=await graphJson<GraphCalendarsPage>(url,accessToken);calendars.push(...page.value);url=page['@odata.nextLink'];}return calendars;}
async function listCalendarEvents(calendar:GraphCalendar,accessToken:string,startAt:string,endAt:string){const params=new URLSearchParams({startDateTime:startAt,endDateTime:endAt,'$select':'id,subject,start,end,isCancelled,isAllDay,showAs,lastModifiedDateTime','$orderby':'start/dateTime','$top':'200'});let url:string|undefined=`${GRAPH_ROOT}/me/calendars/${encodeURIComponent(calendar.id)}/calendarView?${params.toString()}`;const events:ExternalCalendarEvent[]=[];while(url){const page:GraphEventsPage=await graphJson<GraphEventsPage>(url,accessToken);for(const event of page.value){if(event.isCancelled||event.showAs==='free'||event.showAs==='workingElsewhere')continue;const start=asUtcIso(event.start),end=asUtcIso(event.end);if(new Date(end).getTime()<=new Date(start).getTime())continue;events.push({provider:'outlook',calendarId:calendar.id,calendarName:calendar.name??undefined,externalId:event.id,title:event.subject?.trim()||'Busy',startAt:start,endAt:end,isAllDay:Boolean(event.isAllDay),showAs:event.showAs,lastModifiedAt:event.lastModifiedDateTime});}url=page['@odata.nextLink'];}return events;}
export const outlookCalendarProvider:CalendarProvider={id:'outlook',async fetchEvents({accessToken,startAt,endAt}){const calendars=await listCalendars(accessToken);const perCalendar=await Promise.all(calendars.map(c=>listCalendarEvents(c,accessToken,startAt,endAt)));return perCalendar.flat();}};
